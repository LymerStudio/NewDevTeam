name: Send_Result
on:
  workflow_call:
    inputs:
      URL:
        required: true
        type: string
jobs:    
  send-flag-to-chat:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar mensaje a Slack
        env:
          FLAG: ${{ secrets.FLAG }}
        run: |
          SECRETO="$FLAG"
          echo $URL
          curl -X POST -H 'Content-type: application/json' --data '{
            "mensaje": "Todas las condiciones se han cumplido con éxito. Secreto: $SECRETO"
          }' ${{ inputs.URL }}
